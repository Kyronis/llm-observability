import type { Config } from 'tailwindcss';

// Tailwind v4: design tokens live in globals.css @theme block
// This file kept as compatibility stub
const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
};

export default config;
